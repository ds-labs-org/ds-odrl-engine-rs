# ODRL-Test-Suite compliance report

Generated by `compliance-runner` against the vendored [SolidLabResearch/ODRL-Test-Suite](https://github.com/SolidLabResearch/ODRL-Test-Suite) (`compliance/vendor/odrl-test-suite`), adapted into `engine`'s Section 5.2 JSON contract per the adapter's own translation convention (`compliance-runner/src/translate.rs`).

| total | passed | failed | skipped |
|---|---|---|---|
| 68 | 32 | 0 | 36 |

Every vendored fixture policy carries exactly one permission or prohibition rule, so `translate.rs`'s "omit the policy when its sole rule doesn't pertain to this request" convention (see that module's doc comment) is always all-or-nothing here: this corpus never exercises `decide`'s own Section 4.3 open-on-empty-permissions departure as an observable divergence from this suite's closed-world default. A corpus with a policy mixing a matched permission and an unrelated prohibition could still surface that gap as a genuine failure; this one happens not to.

## Failing cases

None.

## Skipped cases

| case | title | Section 7 citation |
|---|---|---|
| testcase-030-alice-read-x | Read request from Alice to resource X returns into yes (temporal eq) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-031-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal eq - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-032-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal eq - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-033-alice-read-x | Read request from Alice to resource X returns into yes (temporal neq) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-034-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal neq - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-035-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal neq - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-036-alice-read-x | Read request from Alice to resource X returns into yes (temporal lt) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-037-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal lt - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-038-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal lt - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-039-alice-read-x | Read request from Alice to resource X returns into yes (temporal lteq) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-040-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal lteq - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-041-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal lteq - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-042-alice-read-x | Read request from Alice to resource X returns into yes (temporal gt) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-043-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal gt - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-044-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal gt - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-045-alice-read-x | Read request from Alice to resource X returns into yes (temporal gteq) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-046-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal gteq - past) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-047-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal gteq - future) (Alice Request Read X). | constraint's odrl:leftOperand is odrl:dateTime; Section 7: "Numeric and date/time comparison operators ... remain unimplemented in the Default Profile (Section 4.2)". |
| testcase-048-alice-read-x | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-049-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X - past). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-050-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X - future). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-051-alice | Read request from people in party collection to resource X returns into yes (Alice Request Read X). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-052-bob-read-x | Read request from people in party collection to resource X returns into yes (Bob Request Read X). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-053-alice-read-x | Read request from Alice to resource in asset collection returns into yes (Alice Request Read X). | odrl:target is an odrl:AssetCollection; this engine's wire contract carries no target/resource concept at all (decision::Rule is {action, constraints} only, Section 5.2), so resource-collection membership cannot be represented — the same class of gap Section 7 names for structured claims and Party functional roles (Section 4.1, 4.4). |
| testcase-054-alice-read-y | Read request from Alice to resource in asset collection returns into yes (Alice Request Read Y). | odrl:target is an odrl:AssetCollection; this engine's wire contract carries no target/resource concept at all (decision::Rule is {action, constraints} only, Section 5.2), so resource-collection membership cannot be represented — the same class of gap Section 7 names for structured claims and Party functional roles (Section 4.1, 4.4). |
| testcase-055-alice-read-x | Read request from people in party collection to resource in asset collection returns into yes (Alice Request Read X). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-056-alice-read-y | Read request from people in party collection to resource in asset collection returns into yes (Alice Request Read Y). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-057-bob-read-x | Read request from people in party collection to resource in asset collection returns into yes (Bob Request Read X). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-059-nonset | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X - duty unknown). | permission carries a per-permission odrl:duty (ODRL's finer pre/post-condition form nested inside one Permission); Section 7: "Per-permission nested duties ... are not modeled: catalog_core::Rule has no nested-duty field" — only policy-level obligations (Section 4.5) are evaluated. |
| testcase-060-fulfilled | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X- duty fulfilled). | permission carries a per-permission odrl:duty (ODRL's finer pre/post-condition form nested inside one Permission); Section 7: "Per-permission nested duties ... are not modeled: catalog_core::Rule has no nested-duty field" — only policy-level obligations (Section 4.5) are evaluated. |
| testcase-061-violated | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X - duty violated). | permission carries a per-permission odrl:duty (ODRL's finer pre/post-condition form nested inside one Permission); Section 7: "Per-permission nested duties ... are not modeled: catalog_core::Rule has no nested-duty field" — only policy-level obligations (Section 4.5) are evaluated. |
| testcase-062-big-policy | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-063-big-policy-OoO | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X - out of office). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-064-big-policy-past | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X - past). | constraint is an odrl:LogicalConstraint (odrl:and/odrl:or/odrl:xone); Section 7: "Nested ODRL logical constraint groups ... remain inherited-limitation out of scope (Section 4.2)" — catalog_core::Constraint only models atomic constraints. |
| testcase-065-alice | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |
| testcase-067-alice-past | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X - past). | odrl:assignee is an odrl:PartyCollection; membership (odrl:partOf) is asserted only in the state-of-the-world graph, not carried in the caller's claims — the flat claims model admits only a top-level string/array-of-string field per claim (Section 4.1), the same representability limit Section 7 names for structured attributes ("Structured PID attributes cannot be carried at all"), and Section 4.4 separately disclaims ODRL's Party functional roles as outside this engine's narrowed Profile Mechanism reading. |

## Passing cases

| case | title | decision |
|---|---|---|
| testcase-001-alice | Any request results into yes (Alice Request). | Allow |
| testcase-002-bob | Any request results into yes (Bob Request). | Allow |
| testcase-003-bob-write-y | Any request results into yes (Bob Request Write). | Allow |
| testcase-004-alice | Any request results into no (Alice Request). | Deny |
| testcase-005-bob | Any request results into no (Bob Request). | Deny |
| testcase-006-bob-write-y | Any request results into no (Bob Request Write). | Deny |
| testcase-007-alice | Any request to use results into yes (Alice Request). | Allow |
| testcase-008-bob | Any request to use results into yes (Bob Request). | Allow |
| testcase-009-bob-write-y | Any request to use results into yes (Bob Request Write). | Allow |
| testcase-010-alice-sell | Any request to use results into yes (Alice Request Sell). | Deny |
| testcase-011-alice | Any request to use results into no (Alice Request). | Deny |
| testcase-012-bob | Any request to use results into no (Bob Request). | Deny |
| testcase-013-bob-write-y | Any request to use results into no (Bob Request Write). | Deny |
| testcase-014-alice-sell | Any request to use results into no (Alice Request Sell). | Deny |
| testcase-015-alice | Any request from Alice to use returns into yes (Alice Request). | Allow |
| testcase-016-bob | Any request from Alice to use returns into yes (Bob Request). | Deny |
| testcase-017-alice-sell | Any request from Alice to use returns into yes (Alice Request Sell). | Deny |
| testcase-018-alice | Any request from Bob to use returns into no (Alice Request). | Deny |
| testcase-019-bob | Any request from Bob to use returns into no (Bob Request). | Deny |
| testcase-020-bob-sell | Any request from Bob to use returns into no (Bob Request Sell). | Deny |
| testcase-021-alice | Read request from Alice returns into yes (Alice Request). | Allow |
| testcase-022-alice-sell | Read request from Alice returns into yes (Alice Request Sell). | Deny |
| testcase-023-alice-read-y | Read request from Alice returns into yes (Alice Request Read Y). | Allow |
| testcase-024-alice-write-x | Read request from Alice returns into yes (Alice Request Write X). | Deny |
| testcase-025-bob-read-x | Read request from Alice returns into yes (Bob Request Read X). | Deny |
| testcase-026-alice-read-x | Read request from Alice to resource X returns into yes (Alice Request Read X). | Allow |
| testcase-027-alice-write-x | Read request from Alice to resource X returns into yes (Alice Request Write X). | Deny |
| testcase-028-alice-read-y | Read request from Alice to resource X returns into yes (Alice Request Read Y). | Deny |
| testcase-029-bob-write-y | Read request from Alice to resource X returns into yes (Bob Request Write Y). | Deny |
| testcase-058-bob-write-y | Read request from people in party collection to resource in asset collection returns into yes (Bob Request Write Y). | Deny |
| testcase-066-bob-sell | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X - past). | Deny |
| testcase-068-bob-write-y-past | Read request from people in party collection to resource X in 2024 returns into yes (Bob Request Write Y - past). | Deny |
