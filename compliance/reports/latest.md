# ODRL-Test-Suite compliance report

Generated by `compliance-runner` against the vendored [SolidLabResearch/ODRL-Test-Suite](https://github.com/SolidLabResearch/ODRL-Test-Suite) (`compliance/vendor/odrl-test-suite`), adapted into `engine`'s Section 5.2 JSON contract per the adapter's own translation convention (`compliance-runner/src/translate.rs`).

| total | passed | failed | skipped |
|---|---|---|---|
| 68 | 66 | 2 | 0 |

Since `engine`'s `odrl:includedIn` action-coverage revision, `translate.rs` no longer pre-filters a policy's rules by action at all — every rule survives translation with its own declared action (or, absent one, the request's own — see that module's doc comment), and `engine::decide` alone decides whether a rule's action covers the request's. Two vendored fixtures (`testcase-014-alice-sell`, `testcase-020-bob-sell`) now fail as a direct, honest consequence: each policy's *only* rule is a prohibition on `use` that does not cover the fixture's `sell` request, leaving that policy's `permissions` list empty — which `decide`'s own Section 4.3 departure treats as *open* regardless of an unrelated, non-covering prohibition being present, producing Allow where this suite's closed-world ground truth expects Deny. Earlier revisions never surfaced this: a translate-time action pre-filter used to discard such a policy's sole rule outright, which happened to route the request through Section 5.2's *different* empty-policies default (closed), masking the divergence rather than resolving it. See `README.md`'s compliance summary for the fuller account; this is a property of the vendored engine's own decision algorithm, not a translation bug, and is not silently worked around here.

## Failing cases

| case | title | expected | actual | reason |
|---|---|---|---|---|
| testcase-014-alice-sell | Any request to use results into no (Alice Request Sell). | Deny | Allow | policy 'urn:uuid:b0d32e25-f642-4afa-9098-0e23750f7ad6' has no permissions (open default) |
| testcase-020-bob-sell | Any request from Bob to use returns into no (Bob Request Sell). | Deny | Allow | policy 'urn:uuid:e4b538e6-2613-4de4-8930-48fee524aa40' has no permissions (open default) |

## Skipped cases

None.

## Passing cases

| case | title | decision |
|---|---|---|
| testcase-001-alice | Any request results into yes (Alice Request). | Allow |
| testcase-002-bob | Any request results into yes (Bob Request). | Allow |
| testcase-003-bob-write-y | Any request results into yes (Bob Request Write). | Allow |
| testcase-004-alice | Any request results into no (Alice Request). | Deny |
| testcase-005-bob | Any request results into no (Bob Request). | Deny |
| testcase-006-bob-write-y | Any request results into no (Bob Request Write). | Deny |
| testcase-007-alice | Any request to use results into yes (Alice Request). | Allow |
| testcase-008-bob | Any request to use results into yes (Bob Request). | Allow |
| testcase-009-bob-write-y | Any request to use results into yes (Bob Request Write). | Allow |
| testcase-010-alice-sell | Any request to use results into yes (Alice Request Sell). | Deny |
| testcase-011-alice | Any request to use results into no (Alice Request). | Deny |
| testcase-012-bob | Any request to use results into no (Bob Request). | Deny |
| testcase-013-bob-write-y | Any request to use results into no (Bob Request Write). | Deny |
| testcase-015-alice | Any request from Alice to use returns into yes (Alice Request). | Allow |
| testcase-016-bob | Any request from Alice to use returns into yes (Bob Request). | Deny |
| testcase-017-alice-sell | Any request from Alice to use returns into yes (Alice Request Sell). | Deny |
| testcase-018-alice | Any request from Bob to use returns into no (Alice Request). | Deny |
| testcase-019-bob | Any request from Bob to use returns into no (Bob Request). | Deny |
| testcase-021-alice | Read request from Alice returns into yes (Alice Request). | Allow |
| testcase-022-alice-sell | Read request from Alice returns into yes (Alice Request Sell). | Deny |
| testcase-023-alice-read-y | Read request from Alice returns into yes (Alice Request Read Y). | Allow |
| testcase-024-alice-write-x | Read request from Alice returns into yes (Alice Request Write X). | Deny |
| testcase-025-bob-read-x | Read request from Alice returns into yes (Bob Request Read X). | Deny |
| testcase-026-alice-read-x | Read request from Alice to resource X returns into yes (Alice Request Read X). | Allow |
| testcase-027-alice-write-x | Read request from Alice to resource X returns into yes (Alice Request Write X). | Deny |
| testcase-028-alice-read-y | Read request from Alice to resource X returns into yes (Alice Request Read Y). | Deny |
| testcase-029-bob-write-y | Read request from Alice to resource X returns into yes (Bob Request Write Y). | Deny |
| testcase-030-alice-read-x | Read request from Alice to resource X returns into yes (temporal eq) (Alice Request Read X). | Allow |
| testcase-031-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal eq - past) (Alice Request Read X). | Deny |
| testcase-032-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal eq - future) (Alice Request Read X). | Deny |
| testcase-033-alice-read-x | Read request from Alice to resource X returns into yes (temporal neq) (Alice Request Read X). | Deny |
| testcase-034-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal neq - past) (Alice Request Read X). | Allow |
| testcase-035-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal neq - future) (Alice Request Read X). | Allow |
| testcase-036-alice-read-x | Read request from Alice to resource X returns into yes (temporal lt) (Alice Request Read X). | Deny |
| testcase-037-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal lt - past) (Alice Request Read X). | Allow |
| testcase-038-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal lt - future) (Alice Request Read X). | Deny |
| testcase-039-alice-read-x | Read request from Alice to resource X returns into yes (temporal lteq) (Alice Request Read X). | Allow |
| testcase-040-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal lteq - past) (Alice Request Read X). | Allow |
| testcase-041-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal lteq - future) (Alice Request Read X). | Deny |
| testcase-042-alice-read-x | Read request from Alice to resource X returns into yes (temporal gt) (Alice Request Read X). | Deny |
| testcase-043-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal gt - past) (Alice Request Read X). | Deny |
| testcase-044-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal gt - future) (Alice Request Read X). | Allow |
| testcase-045-alice-read-x | Read request from Alice to resource X returns into yes (temporal gteq) (Alice Request Read X). | Allow |
| testcase-046-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal gteq - past) (Alice Request Read X). | Deny |
| testcase-047-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal gteq - future) (Alice Request Read X). | Allow |
| testcase-048-alice-read-x | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X). | Allow |
| testcase-049-alice-read-x-past | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X - past). | Deny |
| testcase-050-alice-read-x-future | Read request from Alice to resource X returns into yes (temporal + And (year 2024)) (Alice Request Read X - future). | Deny |
| testcase-051-alice | Read request from people in party collection to resource X returns into yes (Alice Request Read X). | Allow |
| testcase-052-bob-read-x | Read request from people in party collection to resource X returns into yes (Bob Request Read X). | Deny |
| testcase-053-alice-read-x | Read request from Alice to resource in asset collection returns into yes (Alice Request Read X). | Allow |
| testcase-054-alice-read-y | Read request from Alice to resource in asset collection returns into yes (Alice Request Read Y). | Deny |
| testcase-055-alice-read-x | Read request from people in party collection to resource in asset collection returns into yes (Alice Request Read X). | Allow |
| testcase-056-alice-read-y | Read request from people in party collection to resource in asset collection returns into yes (Alice Request Read Y). | Deny |
| testcase-057-bob-read-x | Read request from people in party collection to resource in asset collection returns into yes (Bob Request Read X). | Deny |
| testcase-058-bob-write-y | Read request from people in party collection to resource in asset collection returns into yes (Bob Request Write Y). | Deny |
| testcase-059-nonset | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X - duty unknown). | Allow |
| testcase-060-fulfilled | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X- duty fulfilled). | Allow |
| testcase-061-violated | Read request from Alice to resource X returns into yes, if duty is not violated (Alice Request Read X - duty violated). | Deny |
| testcase-062-big-policy | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X). | Allow |
| testcase-063-big-policy-OoO | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X - out of office). | Deny |
| testcase-064-big-policy-past | Read request from Alice to resource X on weekdays from 9-17 in 2024 returns into yes (Alice Request Read X - past). | Deny |
| testcase-065-alice | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X). | Allow |
| testcase-066-bob-sell | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X - past). | Deny |
| testcase-067-alice-past | Read request from people in party collection to resource X in 2024 returns into yes (Alice Request Read X - past). | Deny |
| testcase-068-bob-write-y-past | Read request from people in party collection to resource X in 2024 returns into yes (Bob Request Write Y - past). | Deny |
